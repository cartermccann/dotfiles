[package]
name = "ceen-live"
version = "0.1.0"
edition = "2021"
description = "Real-time generative ASCII wallpaper renderer for the Ceen signal hand"

[dependencies]
ab_glyph = "0.2"

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
panic = "abort"
